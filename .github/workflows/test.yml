name: Test workflow

on:
  push:

jobs:
  build:
    name: Build 1
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah
      - run: echo "hello world"

  build_2:
    name: Build 2
    runs-on: ubuntu-latest
    steps:
      - run: ls -lah

  build_3:
    name: Build 3
    runs-on: ubuntu-latest
    needs: build_2
    steps:
      - run: ls -lah
