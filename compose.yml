services:
  next:
    build:
      context: .
      target: next
    environment:
      - APP_ENV=dev
    ports:
      - "3000:3000"
    command: ["sh", "-c", "npm install && npm run dev"]
    volumes:
      - ".:/app"
      - "next_node_modules:/app/node_modules"

volumes:
  next_node_modules:
